<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: 运维 | 徐向阳]]></title>
  <link href="http://xuxiangyang.github.io/blog/categories/yun-wei/atom.xml" rel="self"/>
  <link href="http://xuxiangyang.github.io/"/>
  <updated>2016-02-19T16:19:23+08:00</updated>
  <id>http://xuxiangyang.github.io/</id>
  <author>
    <name><![CDATA[徐向阳]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[How_to_rescue_sudo_privilege_on_linux]]></title>
    <link href="http://xuxiangyang.github.io/blog/2016/02/19/how-to-rescue-sudo-privilege-on-linux/"/>
    <updated>2016-02-19T16:09:09+08:00</updated>
    <id>http://xuxiangyang.github.io/blog/2016/02/19/how-to-rescue-sudo-privilege-on-linux</id>
    <content type="html"><![CDATA[<h1>一次运维失误，误删deployer sudo权限</h1>

<p>经验：
不要用usermod来为用户增加组。比较危险</p>

<pre><code>sudo usermod -a -G group user #这是增加
sudo usermod -g group user #这是直接将user的组设置为group
</code></pre>

<p>用<code>gpasswd</code>替代会更安全</p>

<pre><code>sudo `gpasswd -a user group #增加
sudo gpasswd -d user group #移除`
</code></pre>

<p>最后一个sudo 用户的sudo权限消失了怎么办？
两个方案，都需要停服</p>

<h2>方案一，利用服务器自己的recovery mode</h2>

<p>再重启是，上下选择启动项。
<img src="/images/posts/20160219/1.png">
有可能这个recovery mode 在 advance option里。选择 recovery mode
<img src="/images/posts/20160219/2.png">
选root那个，使用root登录
但这个root是一个readonly的系统，需要重新挂在文件系统</p>

<pre><code>mount -rw -o remount /
</code></pre>

<p>然后你就</p>

<pre><code>sudo `gpasswd -a user group`
</code></pre>

<p>就好了</p>

<h2>方案二，利用linux live mode 挂载硬盘，直接修改文件为用户增加sudo权限</h2>

<p>1.制作一个ubuntu desktop的u盘启动盘
2.服务器用u盘启动
3.预览ubuntu（bu an z）
4.执行fdisk找到硬盘，挂载（假设找到了sda1）</p>

<pre><code>sudo fdisk -l
sudo mkdir /mnt/root
sudo mount /dev/sda1 /mnt/root
</code></pre>

<p>5.去修改文件 /etc/sudoers
里面加一行</p>

<pre><code>deployer ALL=(ALL:ALL) ALL
</code></pre>

<p>这个是说给deployer增加sudo权限。以前的做法是把deployer加到sudo这个组里。如果没有洁癖，到这里也可以了。
6.重启服务器，试试<code>sudo ls</code>好不好试
7.好使的话</p>

<pre><code>sudo `gpasswd -a user group`
</code></pre>

<p>8.移掉/etc/sudoers
9. 重新登录，看看是不是好使了</p>
]]></content>
  </entry>
  
</feed>
